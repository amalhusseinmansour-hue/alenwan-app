import 'package:flutter/material.dart';

class SportRelatedTab extends StatelessWidget {
  const SportRelatedTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("محتوى رياضي مشابه سيُعرض هنا لاحقاً"),
    );
  }
}
